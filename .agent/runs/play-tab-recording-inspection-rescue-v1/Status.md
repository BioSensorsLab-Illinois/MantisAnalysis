# Play / Recording Inspection Rescue — Status

Slug: `play-tab-recording-inspection-rescue-v1`
Started: 2026-04-25
Owner: Zhongmin Zhu (BioSensors Lab @ UIUC)

## Mission

Rescue the 4th MantisAnalysis mode (Play / Recording Inspection) after two prior failed attempts. Implement against the Anthropic-hosted design template. Verify on the real dataset at `/Users/zz4/Desktop/day5_breast_subject_1`. Do not claim done without live-browser real-data screenshots.

Plan: [`/Users/zz4/.claude/plans/ultra-plan-ultra-effort-virtual-fountain.md`](../../../../../.claude/plans/ultra-plan-ultra-effort-virtual-fountain.md).

## Milestones

- [x] M0 — Initiative bootstrap (folder + Status / REAL_DATASET_NOTES / BUG_REGRESSION_CHECKLIST / VERIFICATION_LOG)
- [x] M1 — Backend per-frame extraction (`LoadedSource.extract_frame`, `/frames`, `/frame/{i}/...png`) — 31 new unit tests green; full unit suite 133 passed; live real-data verified against `sample_1_view_0_exp_0.1.h5` (frame 0 vs 9 differ; RGB composite + overlay PNGs valid; frame 10 → 404)
- [x] M2 — App-shell wiring (`'play'` mode + ModeRail 4th tile + key `4` + migration updated) — typecheck + Vite build clean; live browser shows 4-tile rail, PLAY active, stub card + buttons, 0 console errors, key `4` switches correctly
- [x] M3 — Empty + SourcesPanel + multi-file load (real data) — all 7 regions render; 3 real H5s loaded showing 30 frames/2 exposures; load-A-then-B, remove-A-keep-B, load-after-remove all green; 0 console errors; bug-regression rows 1, 2, 4, 12 ticked
- [x] M4 — ViewerCard + RGB auto-mode (real data verified) — RGB · HG default renders real tissue sample; NIR · HG with inferno; Gray · HG-Y; side-by-side multi-view; slider updates frame across cards; exactly 7 source-mode options derived from channels; bug-regression rows 6, 7, 8, 10 ticked
- [x] M5 — Timeline + playback (real data verified) — full TimelineStrip with mini-map / slider / play-pause / FPS / Loop; 30-frame multi-source stream plays cleanly; loop ON wraps 29→0; loop OFF hard-stops at 29; stream-follow auto-rebinds views across source boundaries; bug-regression rows 3, 5 ticked
- [x] M6 — Stream Builder + continuity (real data) — modal opens from clickable stream chip; continuity detection identifies real overlap/gap/exposure-mismatch on the dataset; threshold tuning works; warn-dot in chip border; bug-regression rows 4, 9 strengthened
- [x] M7 — Inspector sections (real data) — 8-section card stack (View · Source · Corrections · Display · RGB grading · Overlay · Labels · Advanced); Display threshold/colormap/invert drive canvas; RGB grading sliders drive CSS filter; collapse state persisted per view-type; bug-regression rows 7, 10 strengthened
- [x] M8 — Dark-frame manager (real data) — `Open dark frame…` flow wired; backend averages all frames in dark H5 (M1 logic); DarkFrameRow shows attach state; DRK badge appears in ViewerCard when applied; Inspector's Apply dark correction toggles correctly when has_dark
- [x] M9 — Overlay + image export (real data) — RGB+NIR overlay source mode renders live composite; OVL badge; Export PNG button fetches the rendered bytes and triggers browser download with `{stream}_f{frame:04d}_{mode}.png` filename; bug-regression rows 8, 11 strengthened
- [x] M10 — Video export (real data) — MP4/GIF/PNG-zip all working from real H5; backend `/export/video` route supports rgb_composite/channel/overlay renders; Export Video modal lets user pick format/range/FPS; download filename = server Content-Disposition (`{stream}_f{NNNN}-{NNNN}_{render}.{ext}`)
- [x] M11 — Warning Center + responsive + reviewers + close — Warning Center modal aggregates continuity / file-load / dark-attach errors with severity grouping; layout works at 1100×800 viewport; 2 reviewer agents found 1 P0 + 4 P1; P0s + critical P1s resolved (RLock for race; modifier-key guard; blob revocation; export-frame cap; error-class mapping); 4 P1/P2 deferred to BACKLOG with rationale; full unit suite + smoke tiers 0–3 green

## Phase 2 — Polish + Parity + HDR + Advanced

- [x] M12 — Frame cache + double-buffer + colormap retention (shipped during user feedback after M11). LRU 96-entry blob cache; image stays visible during fetch; play loop prefetches `globalFrame + 2`; colormap not auto-reset on source-mode switch.
- [x] M13 — ViewerCard ⋮ menu + Lock-to-frame + amber pin in mini-map. 6-item overflow popover (Rename / Duplicate / Lock / Include / Copy settings to / Remove); ESC + click-outside dismiss; Lock action captures the view's current local frame and renders an amber caret on the timeline mini-map at the global-frame position; Duplicate clones display state but resets isLocked; Copy-settings-to opens a sub-popover with checkbox list of other views and applies a whitelisted patch (display fields only, never sourceId/sourceMode/name). Verified: lock at frame 5 → caret at 26.3% of 20-frame stream → globalFrame=12 → locked view footer still reads frame 5; LCK badge present; duplicate produced "RGB · HG (copy)" without lock; 0 console errors.
- [x] M14 — 3+1 layout preset. `LAYOUT_PRESETS` gains a 5th entry `{ id: '3plus1', label: '3+1', cols: 'custom', rows: 'custom' }`; `ViewerGrid` branches on `layout.id === '3plus1'` to set `gridTemplateColumns: 1fr / 2fr` and `gridTemplateRows: repeat(max(views.length-1, 1), 1fr)`; per-view `cellStyle` places `views[0]` at `gridColumn: 2; gridRow: 1 / -1` (large) and `views[1..N-1]` at `gridColumn: 1; gridRow: i / span 1` (small column). `ViewerCard` accepts an optional `cellStyle` prop spread into its root style; non-3+1 presets pass nothing and revert to auto-flow placement. Verified: 4 views at 1700×1000 → grid `322.6 / 645.3` cols, 3 rows of 234 px, large card 645×716, 3 small cards 323×235 stacked at y={132, 367, 602}; scrub slider to frame 7 → all 4 footers show "frame 7"; resize to 1200×800 → grid `156 / 312` cols, 168 px rows, layout intact; round-trip 2×2 → 3+1 → 2×2 leaves cells back to `auto`; 3-view edge case (rm one) gives 2 small + 1 large with rows: `355 355`; 1-view edge case still renders the card in the right column without errors; 0 console errors throughout.
- [x] M15 — Auto-collapse Sources / Inspector at narrow viewport. `useViewport` (already exported from `web/src/shared.tsx`) imported into `web/src/playback.tsx`; an effect watches `isNarrow` (`window.innerWidth < 1180`) and on bucket transitions sets both `sourcesCollapsed` and `inspectorCollapsed` to match the new bucket. Per-panel `touchedRef` (session-only) latches when the user clicks the panel's collapse chevron and defers the auto-rule until the next viewport bucket cross. Persisted localStorage values remain the source of truth at mount, so user-manual collapse state survives a reload. Verified: at 1480 px both expanded; resize 1480→1100 + dispatch resize event → both auto-collapse (`mantis/playback/sourcesCollapsed = "true"`, `inspectorCollapsed = "true"`); resize 1100→1480 → both auto-expand (`"false"`); manual click on Sources `Collapse panel` button at 1480 → only Sources collapses (`src=true`, `insp=false`); reload at 1480 → Sources still collapsed, Inspector still expanded; screenshot at 1100 with real H5 loaded shows both panels as 44 px icon rails and the canvas + timeline filling the rest; 0 console errors.
- [x] M16 — Inline FilePill warnings. Backend: `LoadedSource.warnings: List[Dict[str, str]]` field plus `_summary_dict` serialization plus Pydantic `SourceSummary.warnings`; `load_from_path` emits `W-META-TS` when `camera/timestamp` is absent (timestamps fall back to `arange`), `W-META-EXP` when `camera/integration-time` is absent (exposures fall back to zeros), and `W-FRAME-FAIL` when the metadata read raises. Frontend: `loadRecordings` forwards `summary.warnings` into the recording state; `FilePill` renders one yellow `⚠ <code>` chip per warning between the metadata row and the (existing) expanded detail block, click toggles a small detail panel showing the canonical `detail` text + `Inspect in Warning Center` (deep-link → opens the modal) + `Dismiss` (session-only per-source dismissal that hides the chip). FilePill border switches to `t.warn` when at least one undismissed warning is live. `WarningCenterModal` aggregates per-source warnings alongside continuity / dark / load errors with `source: 'file'` + `file: <name>` so the modal stays the canonical view (dismissal is local to the FilePill chip only). StreamHeader's overall warning count includes per-source warnings. Tests: 5 new unit tests in `tests/unit/test_session_frames.py` (`_make_synthetic_h5_missing` builder; complete H5 emits zero warnings; missing-timestamp emits W-META-TS only; missing-integration-time emits W-META-EXP only; missing both emits both; `_summary_dict` round-trip carries warnings). Verified live: synthetic `/tmp/m16_no_ts.h5` (no `camera/timestamp`) loaded alongside real `sample_1_view_0_exp_0.025.h5` → m16 pill shows the yellow `W-META-TS` chip + warn-coloured border; clicking the chip opens the detail row with both buttons; Inspect opens Warning Center with the same row labelled `source: file · file: m16_no_ts.h5`; Dismiss removes the chip but leaves the WC entry intact; healthy-only-load shows `warnCount=0` and no chips; 148/148 unit tests + Tier 0 + Vite build all green; 0 console errors.
- [x] M17 — Range-brush on main timeline mini-map. PlaybackMode owns `rangeSelection: [start, end] | null` (null = no brush, full stream is the export default; `[start, end]` = brush active, drives ExportVideoModal). TimelineStrip gains: a "Range" toggle pill on the right side of the mini-map header (creates a brush at `[0, totalFrames-1]`, clicked again to clear), two amber drag handles on the mini-map with a translucent amber fill between them, window-level mousemove/mouseup capture so drag continues past the mini-map's bounds, click-to-jump on the mini-map ignores clicks landing on a handle. ExportVideoModal accepts `rangeSelection` + `onClearRange`: when active, Spinbox start/end are disabled+readOnly, an inline amber pill explains "Frames 5–9 (5 frames) from the timeline range brush" with a Clear button that drops the brush and re-enables the inputs. `Spinbox` (shared.tsx) gains a `disabled` prop. PlaybackMode's totalFrames-clamp effect drops the brush if the stream shrinks past its bounds. Verified live: 15-frame stream loaded, click Range pill → handles + fill render at extremes (`Range 0–14`); drag start handle to ~5/14 then end to ~9/14 → label updates to `Range 5–9`; open Export Video modal → start=5/end=9 inputs disabled+readOnly, locked pill shows "Frames 5–9 (5 frames)…"; clicking Export fires `/api/sources/{sid}/export/video?format=zip&start=5&end=9&fps=10&...`; close modal, click the locked-pill's Clear → modal start=0/end=14, Spinboxes re-enabled, locked-pill gone, brush handles disappear from the timeline; 0 console errors throughout.
- [x] M18 — 12 canonical W-* warning IDs with templates + actions. New module-level `WARNING_TEMPLATES` map in `web/src/playback.tsx` keyed by code with `{severity, headline, action: {kind, label}}` covering all 12 from the plan (W-META-TS · W-META-EXP · W-EXP-MISMATCH · W-SHAPE · W-CHAN · W-DARK-NONE · W-DARK-AMBIG · W-OVL-SRC · W-EXPORT-LONG · W-FRAME-FAIL · W-PROC-INVALID · W-RANGE-HUGE) plus the legacy emit codes (W-GAP · W-OVERLAP · W-LOAD · W-DARK). Action kinds: `inspect-file` · `open-stream-builder` · `clear-range` · `open-export` · `open-inspector` · `dismiss`; the parent maps them to closing the modal + opening the right surface. `detectContinuity` renamed `type: 'exposure' → 'exp-mismatch'` and added a channel-set diff that emits `type: 'chan'`; the WC aggregation maps both into the canonical W-* codes. Frontend derivations added in PlaybackMode's WC payload: W-OVL-SRC (overlay view whose overlay channel isn't on the bound recording), W-PROC-INVALID (vmin ≥ vmax), W-DARK-AMBIG (view has `applyDark` true but recording has no dark attached), W-RANGE-HUGE (rangeSelection covers > 1000 frames), W-EXPORT-LONG (totalFrames > 1500). WarningCenterModal refactored: each row now renders `code · headline (bold) · detail · source/file · [action button]`, with the action button wired to a parent `onAction(kind, w)` handler. Verified live with synthetic m16_no_ts.h5 + real GSense H5: 7 codes triggered organically (W-SHAPE / W-GAP / W-EXP-MISMATCH / W-META-TS / W-PROC-INVALID after vmin>vmax / W-DARK-AMBIG after switching view to overlay / W-DARK-NONE), each with the canonical headline + matching action button label; clicking W-SHAPE's `Open Stream Builder` action correctly closes the WC and opens the Stream Builder modal. Remaining 5 codes (W-OVL-SRC for non-existent overlay channel, W-RANGE-HUGE for a 1000+ frame brush, W-EXPORT-LONG for a 1500+ frame stream, W-CHAN for channel-set-mismatched files, W-FRAME-FAIL for a corrupt H5) emit-paths are wired and would fire on their natural deliberate inputs; not all reachable in this session's fixture set. 0 console errors.
- [x] M19 — Stream Builder polish (quarantine + threshold info-icon + per-row metadata expand). `StreamBuilderModal` gains: (1) per-source `quarantined: Set<source_id>` state — sources stay in the Sources panel but are excluded from the active stream draft; (2) shape-mismatch detection (compare each row's `shape` to the first surviving row → red border + `Quarantine` button on offenders); (3) a "Quarantined files (N)" section below the file list with a `Restore` button per row that returns the source to the active draft; (4) a `Tip` info-icon next to the Continuity threshold input explaining the merge-vs-flag rule; (5) per-row chevron expand → an inline monospace metadata block (Frames · Dimensions · Channels · Exposure · Timestamp range · FPS (est) · Size); (6) Summary card adds a Quarantined: N (excluded) row when non-empty; (7) Apply payload filters out quarantined sids so PlaybackMode only sees the curated stream order. Verified live with the GSense `sample_1_view_0_exp_0.025.h5` (512×512) + a synthetic 32×64 H5 (`/tmp/m19_shape_mismatch.h5`): synthetic row gets `data-shape-mismatched="true"` + Quarantine button; click Quarantine → active row count 2→1, Total frames 14→10 (only the real H5), Summary shows "Quarantined 1 (excluded)", shape warning gone; click chevron on the surviving row → metadata panel shows "Frames 10 · Dimensions 512×512 · Channels HG-B,HG-G,HG-NIR,HG-R,HG-Y,LG-B,LG-G,LG-NIR,LG-R,LG-Y · Exposure 25.0 ms · Timestamp range 1771577589.74–1771577590.15 s · FPS (est) 22.03"; click Restore → row count 1→2, mismatched row regains its Quarantine button, Quarantined section gone; threshold tooltip carries the canonical "Gaps larger than this between adjacent recordings are flagged as W-GAP warnings…" text; 0 console errors throughout.
- [x] M20 — Display section: Histogram + clipping toggle + Corrections sliders. Backend: NEW `mantisanalysis/session.py::channel_histogram(image, bins=64)` returns `{counts[64], edges[65], min, max, p1, p99}`; NEW route `GET /api/sources/{sid}/frame/{i}/channel/{ch}/histogram?bins=64` (404/409/500 mirroring the thumbnail route); `channel_to_png_bytes` accepts `show_clipping: bool` — when true, lifts the output to RGB and paints magenta `#FF00FF` on every pixel where the underlying value ≥ the (resolved) high threshold; the per-frame channel thumbnail route accepts `?show_clipping=true` and forwards. Frontend: NEW `HistogramPanel` component fetches the new route on (source_id, channel, localFrame) change and renders an inline 220×56 `<svg>` with 64 bars + dashed p1/p99 reference lines + solid amber `data-histogram-vmin` / `data-histogram-vmax` markers; channel-selection helper `histogramChannelFor(view, recording)` picks `meta.channel` for channel modes, `view.rawChannel` for raw, the overlay channel for overlay views, and HG-G/LG-G as a representative for RGB modes. Inspector Display section: gains a `Show clipped pixels` Checkbox (`data-inspector-show-clipping`) under Invert, then the HistogramPanel. Inspector Corrections section: gains `Gain` / `Offset` / `Normalize (Auto · Manual · None)` spinboxes plus a Defaults button (frontend-only state for now; server round-trip lands at M22). `frameChannelPngUrl` and `buildFrameUrl` forward `show_clipping`; `makeDefaultView` initializes `showClipping=false`, `gain=1.0`, `offset=0`, `normalize='auto'`. Tests: 6 new unit tests in `tests/unit/test_play_routes.py` covering 64-bin histogram round-trip, 404 on bad channel, 404 on out-of-range frame, 422 on bins<4, magenta-pixel-present after `?show_clipping=true&vmin=0&vmax=10`, and L-mode preserved when neither colormap nor clipping is set. Verified live (real GSense H5, NIR · HG view): histogram renders 64 bars + p1/p99 dashed lines (`barsCount: 64`); setting Low=1500/High=12000 via Inspector spinboxes moves the amber `vmin`/`vmax` lines on the histogram; toggling Show clipped pixels fires `?show_clipping=true` on the next thumbnail request and the canvas visibly paints magenta on saturated tissue regions; 154/154 tests + Tier 0 + Vite build all green; 0 console errors.
- [x] M21 — Bundle JetBrains Mono + backend label burn-in. Vendored `mantisanalysis/static/fonts/JetBrainsMono-Regular.ttf` (273 KB, JetBrains Mono v2.304, SIL OFL) + `OFL.txt`; `pyproject.toml::[tool.setuptools.package-data]` now includes `static/fonts/*.ttf` + `*.txt` so wheels ship the font. NEW `mantisanalysis/labels.py::render_labels(image_rgb, cfg) -> ndarray` (PIL helper) supports timestamp / frame / channel / source_file / scale_bar / processing_badges fields plus position (bottom-left|bottom-right|top-left|top-right) + font_size; renders text in JetBrains Mono with a 1-px black halo on a translucent dark backdrop, optional scale bar in the opposite corner. Per-frame channel + rgb routes accept `?labels_timestamp / labels_frame / labels_channel / labels_source / labels_scale_bar / labels_position / labels_font_size`; backend short-circuits to the unmodified PNG when no flag is set. New `_maybe_burn_labels` helper re-decodes the PNG, calls `render_labels`, re-encodes; pulls `ts_value` from `src.per_frame_timestamps[idx]` and source filename from `src.name` so the frontend doesn't have to round-trip them. Frontend: `view.labels` struct (defaults all-off) initialized in `makeDefaultView`; new `_appendLabelsQuery` helper threads non-default fields into both `frameChannelPngUrl` and `frameRgbUrl`; `buildFrameUrl` lifts `view.labels` into opts. Inspector Labels section replaces the placeholder with checkboxes (Timestamp · Frame index · Channel name · Source file · Scale bar) + a Position select + Font size spinbox. Tests: NEW `tests/unit/test_labels.py` (13 tests) — bundled-font present, no-op when config is None / empty / all-False, deterministic render bytes, doesn't crash on 1×1 image, non-RGB passthrough, anchor moves with `position`, every position renders without crash, timestamp without value falls back to placeholder. Verified live: real GSense H5 frame 0, toggling Timestamp + Frame index in the Inspector Labels section produces a network request `…/rgb.png?gain=hg&max_dim=1600&labels_timestamp=true&labels_frame=true` and the canvas now shows `frame 0` and `ts 1771577589.74 s` burned into the bottom-left corner in JetBrains Mono on a translucent dark backdrop; 0 console errors.
- [x] M22 — Server-side per-channel RGB grading + 5 user-bug hotfix sweep. NEW `mantisanalysis/rgb_grading.py::apply_grading(rgb_arr, params) -> ndarray` operates in float32 [0,1] with the full photographic chain: per-channel offset → per-channel gain → WB Kelvin (Tanner Helland approximation, normalized so D65 = (1,1,1)) → gamma → brightness → contrast → saturation. NEW `auto_white_balance(rgb_arr, method='gray-world'|'max-rgb')` returns suggested gain_r/g/b with G pinned at 1.0, clamped to [0.1, 10.0]. NEW route `POST /api/sources/{sid}/frame/{i}/rgb/auto-wb?gain={hg|lg}&method={gray-world|max-rgb}` returns the JSON suggestion. `_composite_rgb_array` and `_build_rgb_composite_png_from_channels` accept `grading: Optional[Dict]` and apply after the M20.1 ISP chain; both `frame_channel_thumbnail` (`?rgb_composite=true`) and `frame_rgb_composite` accept `gain_r/g/b`, `offset_r/g/b`, `gamma_g`, `brightness_g`, `contrast_g`, `saturation_g`, `wb_kelvin` query params. Frontend Inspector RGB grading section replaces the CSS-filter brightness/contrast/saturation with the full per-channel set + WB Kelvin slider (3000–10000 K) + Auto-WB button + Defaults. View state grows `view.grading = {gain_r, gain_g, gain_b, offset_r, offset_g, offset_b, gamma, brightness, contrast, saturation, wb_kelvin}`. NEW `_appendGradingQuery(q, grading)` URL helper (skips fields at default to keep URLs / cache stable). 13 new tests in `tests/unit/test_rgb_grading.py` (no-op semantics, per-channel isolation, gamma direction, saturation collapse, Kelvin shifts at warm/cool/D65, gray-world neutralizes red cast, clamping safety) + 4 new in `tests/unit/test_play_routes.py` (auto-wb route shape, 404, grading round-trip differs from default, WB Kelvin round-trip differs).

  **Bundled hotfix sweep** for 5 user-reported bugs:
  - **HG vs LG normalization (CRITICAL)**: Backend `_norm_to_unit` and `channel_to_png_bytes` now treat `mode` as the *default-bound* selector — `'none'` defaults to `[0, dtype_max]`, `'auto'` to percentile, `'manual'` to percentile-with-explicit-overrides. User-supplied vmin/vmax always override on that side regardless of mode (so the Display Low/High threshold sliders effective in every mode). **Default normalize is now `'none'`** in `makeDefaultView`, the backend channel/RGB routes, `_composite_rgb_array`, and `_build_rgb_composite_png_from_channels` so HG (bright) and LG (dim) render at their *true* relative brightness without auto-percentile boost. Operational evidence: `gain=hg` luma=61.9, `gain=lg` luma=9.1 (Δ=52.8) at default; `?vmin=0&vmax=20000` on HG → luma=159.0 (thresholds clip + rescale). Overlay route diverges to `mode='auto'` for both base and overlay since overlays are visualization aids, not WYSIWYG.
  - **Display section illogical for RGB**: Inspector Display section now conditional on `sourceModeMeta(...).kind`. RGB views see only Show clipped pixels + Histogram on frame + the inline histogram (with an explainer pointing to RGB grading); channel/raw/overlay views see the full set (Low/High threshold, Auto Reset, Colormap, Invert, Show clipped, Histogram on frame, histogram).
  - **Checkbox value→checked**: All 6 Play-mode `<Checkbox value=…>` callsites renamed to `checked=…` so visual state matches view state.
  - **Apply-dark checkbox enabled when no dark loaded**: Visual `checked` state now AND-clamped with `selectedRecording?.has_dark`, so the box reads OFF when no dark is attached even though `view.applyDark` defaults to `true`.
  - **Duplicate brightness/contrast/gamma sliders in Corrections**: Removed; those parameters now live exclusively in RGB Grading (per-channel R/G/B chain). Corrections retains Apply dark, Black level, Gain, Offset, Normalize + Defaults.
  - **TopBar header shows file from another mode**: TopBar source chip hidden when `mode === 'play'`. Play mode owns its own multi-source state (recordings[]) and renders its own Stream Header chip.

  Verified live: real GSense H5 → top-bar shows no stale chip; HG-vs-LG luma gap is 52.8 at default (was -0.5 with auto-boost); thresholds work in every normalize mode; Display section renders only the explainer + Show clipped pixels + Histogram on frame + inline histogram on RGB views; Corrections has no duplicate sliders; Apply-dark checkbox unchecked when no dark loaded; 0 console errors. Final test count: **191/191** (was 167; +13 grading + +4 route + +7 from bug-fix retrofitting).
- [x] M23 — Tiled image export (5 layout presets). NEW Pydantic models `TiledExportViewSpec` + `TiledExportRequest`; NEW route `POST /api/sources/export/image-tiled` accepts `{views[], layout: 'auto'|'1xN'|'2xM'|'3plus1'|'4x2', gap_px, background, max_dim, format: 'png'|'tiff'|'jpeg'}` and returns the composed image with `Content-Disposition: attachment`. Internal `_render_view(spec)` routes per spec.render to the rgb_composite / channel / overlay path with all M20.1 ISP + M21 burn-in label + M22 RGB grading params honored — exported tiles match the live ViewerCard render byte-for-byte (modulo PNG re-encode). Layout placement: each cell sized by max(tile width/height) within its row/col so nothing crops; `3plus1` puts views[0] in the right column spanning every row, views[1..] in the left column row-by-row. Cap of 16 views (HTTP 413 over). Frontend: NEW `ExportImageModal` with scope (All visible / Selected only / Custom checklist), layout (5 presets), format (PNG/TIFF/JPEG), gap (0–64 px), background (hex). NEW `buildTiledViewSpec(view)` mirrors `buildFrameUrl` per-field → tiled spec. NEW `exportImageTiled` POSTs the request, downloads the result. StreamHeader's `+ PNG` button now opens the modal when `views.length > 1`; single-view stays on the legacy `exportImage` flow. Tests: 6 new in `tests/unit/test_play_routes.py` (400 on empty views, 413 on >16 views, 404 on unknown source, valid PNG with Content-Disposition + magic bytes, layout selectors produce different canvas sizes, JPEG format produces JPEG SOI bytes). Verified live: 4-view 2×2 (RGB·HG / NIR·HG inferno / Gray·HG-Y / RGB·LG) → 1048×1048 PNG, 2 MB, returned with `play_tiled_4views_2xm.png` filename; visual screenshot shows all 4 tiles distinct with 12 px gaps over the dark background; 0 console errors. Smoke: tier 0 PASS, pytest 197/197 (was 191; +6 new), Vite build clean.
- [x] M24 — Tiled video export + range brush wired. NEW Pydantic `TiledExportVideoRequest` (extends the M23 image-tiled spec with `start`, `end` (inclusive global anchors), `fps`, `format: 'mp4'|'gif'|'zip'`). NEW route `POST /api/sources/export/video-tiled` iterates global anchor frames in `[start, end]`, calls the extracted module-level `_render_tiled_view_to_rgb(spec, *, max_dim, override_frame_index)` for each view per frame, composites via the new module-level `_compose_tiled_canvas(tiles, *, layout, gap_px, background)`, and encodes as MP4 (libx264, even-pad, yuv420p) / GIF (imageio) / PNG-zip (one PNG per frame in a ZIP_STORED archive). Each view's `frame_index` is the anchor for the first output frame; subsequent frames advance in lock-step (clamped to each source's own `frame_count` so streams of differing length don't overshoot). Hard caps: 16 views (HTTP 413), 1000 frames per export (HTTP 413). Refactor: extracted M23's per-view render + per-canvas compose helpers from inside the route closure to module level so both routes share the same code path. Frontend: `exportVideo` callback in PlaybackMode now branches on `views.length > 1` — single-view stays on the legacy `/export/video` URL path, multi-view POSTs `{views, layout, gap_px, background, fps, start, end, format}` to the new tiled endpoint. ExportVideoModal accepts `viewCount` and renders a layout chooser (Auto / 1×N / 2×M / 3+1 / 4×2) only when `viewCount > 1`; `layout` and `tiled` flow into the export payload. M17's `rangeSelection` brush already wires into the modal as Start/End locked-pill — the tiled export honors it. Re-ordered `buildTiledViewSpec` to be declared BEFORE `buildVideoUrl` to avoid TDZ on the dependency array. Tests: 6 new unit tests in `tests/unit/test_play_routes.py` (400 on empty views, 404 on unknown source, ZIP emits one PNG per frame, layout selector actually changes per-frame composite size, 413 on >16 views, end clamps to source frame_count). Verified live: real GSense H5 with 4 views (RGB·HG / RGB·LG / Gray·HG-Y / NIR·HG / etc.) at 2×2 layout, range brush set to frames 1–3 → ExportVideoModal shows "Frames 1–3 (3 frames) from the timeline range brush" + Clear button + 5 layout buttons + 3 format buttons; click Export → POST `/api/sources/export/video-tiled` returns 200 (zip path); direct API probes confirm MP4 path (178 KB, 5 frames, 2×1 layout, valid `ftyp` box, `Content-Disposition: attachment; filename="play_tiled_2views_1xn_f0000-0004.mp4"`) and ZIP path (6 MB, 3 frames, 2×2 layout, valid PNGs inside, filename `play_tiled_4views_2xm_f0000-0002.zip`). Smoke: tier 0 PASS, pytest 203/203 (was 197; +6 new), Vite build clean. **Group R · Render parity is now complete.**
- [x] M25 — HDR ISP mode (saturation-aware switch + Mertens). NEW `mantisanalysis/hdr_fusion.py` — pure-NumPy module exposing `fuse_hdr(hg_arr, lg_arr, params) -> ndarray` (modes: `'switch'` saturation-aware hard switch with default threshold 60000 + gain ratio 16.0; `'mertens'` Reinhard-style smooth blend with configurable `knee_width`) and `add_hdr_channels(channels, *, params)` that mutates a channel dict in-place to add `HDR-{R, G, B, NIR, Y}` (Y is Rec. 601 luma over the fused R/G/B). HDR fusion is wired into every RGB-NIR emit site (load-time `image_io.load_recording_channels`, per-frame `LoadedSource.extract_frame`, ISP reconfigure, dark-frame load) so the channel set always carries HDR-* alongside HG-/LG-. Backend `_composite_rgb_array` and the per-frame channel/RGB routes recognize the `HDR-` prefix; `gain` query param accepts `hg|lg|hdr` and the prefix-derivation handles all three; `TiledExportViewSpec.gain` documents `hdr` as a valid value. Frontend `SOURCE_MODES` gains 3 new entries — `RGB · HDR` (kind=rgb, gain=hdr, requires HDR-R/G/B), `NIR · HDR` (kind=channel, channel=HDR-NIR, defaultColormap=inferno), `Gray · HDR-Y` (kind=channel, channel=HDR-Y, defaultColormap=gray). All three light up automatically when HDR-* channels are present in `recording.channels`. Tests: 12 new in `tests/unit/test_hdr_fusion.py` (switch keeps HG below threshold, switch replaces HG above threshold with LG·R, Mertens blends smoothly, Mertens at threshold ≈ LG·R, defaults use switch, shape-mismatch raises, uint16 input handled, add_hdr_channels emits 5 channels with correct shapes, HDR-Y ≈ Rec. 601 luma, no-op on partial dicts, SessionStore.extract_frame emits HDR-* keys, _summary_dict lists HDR-* in `channels`). Verified live: real GSense H5 → backend `recording.channels` includes HDR-{B, G, NIR, R, Y}; source-mode dropdown lists `RGB · HDR`, `NIR · HDR`, `Gray · HDR-Y`; switching to `RGB · HDR` fires `?gain=hdr` and the canvas renders tissue with HDR-fused colors (highlights properly recovered, not blown white as in pure HG); luma-comparison probes confirm HG=57.8, LG=58.3, HDR=50.4 — distinct from both gain branches, validating the fusion math. Smoke: tier 0 PASS, pytest 215/215 (was 203; +12 new), Vite build clean. **Group H · HDR + ISP-chain parity is in flight (M25 ✓; M26-M27 next).**
- [x] M26 — Sharpen / FPN ISP chain in Play. Backend: NEW `_isp_chain_from_query` helper packages the 8 non-linear stages (sharpen_method/amount/radius, denoise_sigma, median_size, gaussian_sigma, hot_pixel_thr, bilateral) into an `ISPParams` (with `black_level=0` to avoid double-subtraction with the linear `_apply_pre_norm` chain). The per-frame channel route (`frame_channel_thumbnail`), the per-frame RGB route (`frame_rgb_composite`), and the tiled-export renderer (`_render_tiled_view_to_rgb`) all accept the 8 query params and apply the chain after dark-subtract and before linear pre-norm. `_composite_rgb_array` and `_build_rgb_composite_png_from_channels` gain an `isp_pre_chain: Optional[ISPParams] = None` kwarg so the chain runs per-channel on R/G/B (matching USAF's ordering). `TiledExportViewSpec` gains 8 new fields with USAF defaults so `Export Image` and `Export Video` bake in the same processing. Frontend: NEW `_appendIspChainQuery(q, isp)` URL helper (skips defaults to keep URLs / cache stable); `frameChannelPngUrl`, `frameRgbUrl`, `buildFrameUrl`, and `buildTiledViewSpec` thread the new `view.isp` struct through. NEW `IspChainDisclosure` component renders an inline collapsible sub-section inside Corrections with the 8 controls (Sharpen Select · Sharpen amount/radius Spinboxes · Denoise σ · Median size · Gaussian σ · Hot pixel σ · Bilateral · Defaults) plus a green ON badge that lights up when at least one stage is active. Open/closed state persists per-view-type via `useLocalStorageState`. Tests: 5 new in `tests/unit/test_play_routes.py` (sharpen vs baseline byte-difference; FPN smoothing byte-difference; RGB-route sharpen byte-difference; tiled-export sharpen byte-difference; no-op default-params guard byte-identical to pre-M26 baseline). Verified live: real GSense H5 (`sample_1_view_0_exp_0.025.h5`, 10 frames, 512×512), Gray·HG-Y view → set Sharpen=Unsharp mask, amount=2.0 → network shows `?sharpen_method=Unsharp+mask&sharpen_amount=2` returning 200 OK; canvas visibly enhances tissue edges. Reset Defaults → ON badge clears. Set median_size=5 + gaussian_sigma=2.0 → network shows `?median_size=5&gaussian_sigma=2`; canvas renders FPN-smoothed. Switch to RGB·HG with Unsharp mask → `/rgb.png?gain=hg&...&sharpen_method=Unsharp+mask` 200 OK. 0 console errors. Smoke: tier 0-3 PASS, pytest 220/220 (was 215; +5 new), Vite build clean.
- [x] M27 — HDR baseline tests. M25 already shipped 12 tests covering switch + Mertens, default params, shape mismatch, uint16 input, `add_hdr_channels` shape + Rec.601 luma + idempotence, and SessionStore + summary integration. M27 adds 1 final integration test: `test_hdr_y_over_mixed_saturation_patch_matches_switch_expectation` builds a 32×32 fixture with one half clipped at 65000 and one half well below saturation, then asserts the post-fusion HDR-Y matches the Rec.601 luma over HG on the unclipped half AND over LG·ratio (with a +200 recovery margin) on the clipped half — proving the full HG → switch → HDR-Y pipeline at integration scope. `pytest tests/unit/test_hdr_fusion.py -q` green at **13/13**. **Group H · HDR + ISP-chain parity is now COMPLETE.**
- [x] M28 — Server-side presets store. Backend: NEW Pydantic `PlaybackPreset` (`id`, `name`, `view_type`, `fields: Dict[str, Any]`, `created_at`) + `PlaybackPresetsBody` request model. NEW module-level helpers `_playback_presets_path()` (uses `MANTIS_PRESETS_PATH` env var override for tests; default `~/.mantisanalysis/playback-presets.json`), `_load_playback_presets()` (graceful `[]` return on missing/corrupt file, per-row try/except so one malformed entry doesn't kill the whole list), `_save_playback_presets(...)` (atomic tmp-write → fsync → `os.replace`; mode 0600 on the final file; lock-protected). NEW routes: `GET /api/playback/presets`, `PUT /api/playback/presets` (full-replace semantics), `DELETE /api/playback/presets/{preset_id}` (idempotent — 200 OK even when id wasn't present). Frontend: PlaybackMode owns `presets` state plus `replacePresets`, `savePreset`, `deletePreset` callbacks. NEW `PRESET_FIELDS` whitelist captures display + ISP + grading + labels + overlay fields (excludes identity: `id` / `sourceId` / `sourceMode` / `name` / `lockedFrame` / `isLocked` / `includedInExport`). NEW `PresetsList` component (Inspector "Presets" InspectorSection between Labels and Advanced) shows the saved presets matching the current view's `sourceMode` with Load + two-click Delete. NEW `SavePresetModal` (single name input, Enter/Save/Cancel). View-type filter ensures applying a preset always lands on a compatible target. Tests: 14 new in `tests/unit/test_presets_api.py` (empty-list on missing file; persisted-list round-trip; malformed rows skipped; corrupt JSON returns empty; PUT creates file; PUT/GET round-trip; PUT replaces existing; mode 0600 verified on POSIX; pretty-printed JSON; DELETE removes by id; DELETE unknown id idempotent; DELETE on empty store; PUT validation rejects missing required field; PUT accepts arbitrary nested JSON in `fields`). Verified live on real GSense H5: Save modal opens via Inspector → Presets → Save…; typing "high-contrast NIR" + Save fires PUT; the row appears in the Presets list with the right name; backend `~/.mantisanalysis/playback-presets.json` carries the full whitelisted view snapshot; DELETE round-trips via API; disk file has mode `-rw-------` (0600) on real filesystem. 0 console errors. Smoke: tier 0–3 PASS, pytest **235 / 235** (was 221; +14 new presets-API tests), Vite build clean.
- [x] M29 — Overlay Builder modal (4-step wizard) **+ legacy-h5 P0 fix sweep**. Frontend: NEW `OverlayBuilderModal` (4 steps · Base → Source → Thresholds → Preview & Apply) backed by an internal draft state, with live preview pane on the right (uses the same `frameOverlayUrl` builder as the inline configurator), step indicator with active/done coloring, Back/Next/Apply footer, and Apply commits the draft to `view.overlay`. Reachable via "Open Overlay Builder…" button injected at the top of `OverlayConfigurator` (visible only when source-mode is overlay-kind). State threading: PlaybackMode owns `overlayBuilderViewId`; Inspector forwards `onOpenOverlayBuilder` callback; modal mounts at root next to SavePresetModal. Cancel + Back disabled gating prevents committing on incomplete drafts. **Bundled P0 fix (user-reported regression)**: Legacy gsbsi-prefix H5 files (e.g. `gsbsi_2019-02-22-T053025_003.h5`) played back at ~1 fps because the legacy container `/dset` is un-chunked HDF5 storage with shape `(H*W, N)` — per-column `dset[:, idx]` reads scattered bytes across the entire 537 MB file. `LegacyFrameReader._ensure_open` now slurps the whole `/dset` into RAM once via `np.asarray(ds[...])`; subsequent reads are pure NumPy slices (~10 ms/frame instead of ~1000 ms). Plus: NEW `LoadedSource.raw_shape: Tuple[int, int]` + `raw_bit_depth: int` + `raw_dtype: str` fields, populated for both modern (raw mosaic from `load_any_detail`'s `raw` array) and legacy (`LEGACY_FRAME_HW`, 12-bit) paths; surfaced in `_summary_dict` and `SourceSummary` Pydantic; FilePill displays the raw mosaic resolution + bit-depth chip ("`1024×2048 · 12b`" for legacy, "`4096×2048`" for modern), ViewerCard footer carries the per-channel rendered resolution. Verified live on the user's legacy file: 128 frames load in 60 ms (one-time slurp), 30 parallel rgb.png fetches return in 445 ms (15 ms/frame), playback at 10 fps wraps cleanly through 128 frames + back to 0 with no perceptible stutter, FilePill chips W-META-TS + W-LEGACY-12BIT, 0 console errors.
- [ ] M30 — Right-click frame → Analyze in USAF/FPN/DoF
- [ ] M31 — Loading-metadata screen (W2)
- [ ] M32 — 15-screen scenario switcher
- [ ] M33 — Playwright web smoke F1–F14
- [ ] M34 — Final reviewer pass + close

## Current state

**Phase 1 — Rescue: COMPLETE. Phase 2 — Polish + Parity + HDR + Advanced: in flight.**

Phase 1 (M0–M11) closed; all 12 prior-failure rows ✓. Phase 2 milestones M12–M29 done — **Groups P + R + H complete; Group A in flight (M28 + M29 ✓; M30 next)**.

Pickup pattern: sequential milestones — implement and verify the next unchecked one, then continue or stop based on remaining context and risk. The one-milestone-per-session contract has been removed; multiple milestones may ship in a single session when scope and risk allow. Read the plan file at `/Users/zz4/.claude/plans/ultra-plan-ultra-effort-virtual-fountain.md` for the full milestone catalog.

## Decisions locked

1. MP4 video export = first-class. Add `imageio[ffmpeg]>=2.30` to `pyproject.toml`.
2. Continuity threshold default = 1.0 s.
3. Loop = ON by default; user can disable for hard stop.
4. Exports → browser download (`Content-Disposition: attachment`).
5. Existing top-bar Sample button unchanged. New "Load Play sample" entry in command palette + Play empty state.

## Open risks

- Real H5 frames are 2048×4096 uint16 (≈16 MB) — frame cache budget needs to bound at ~4 frames × 16 MB = 64 MB to keep RSS sane.
- No dark-frame H5 in the real dataset folder. Dark UI must handle empty as a normal warning.
- Browser HTTP cache must be exploited via stable URLs; verify `Cache-Control` doesn't block it.

## Next concrete action

M30 — Right-click frame → Analyze in USAF/FPN/DoF. Per plan §M30: NEW `mantisanalysis/session.py::create_transient_from_frame(parent_sid, frame_idx)` returns a new image-kind source whose channels are the parent's `extract_frame(idx)` output, marked transient (auto-evicted after 30 min idle). NEW `POST /api/playback/handoff` accepting `{source_id, frame_index, target_mode}` → returns the transient `source_id`. Frontend ViewerCard `onContextMenu` opens a popover with USAF/FPN/DoF targets; click → POST handoff → window event `mantis:switch-source`. Gate: load real H5 → scrub frame 5 → right-click → "Send to USAF" → app switches modes with synthetic source loaded; original recording stays intact in Play.
